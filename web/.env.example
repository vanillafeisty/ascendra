# Ascendra Web — Environment Variables
# Copy to .env.local

# URL of your running backend (no change needed for local dev)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# No Anthropic key needed — AI runs via Groq in the backend (free!)
